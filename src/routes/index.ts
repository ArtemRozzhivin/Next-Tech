const routes = Object.freeze({
  main: '/',
  signup: '/signup',
  signin: '/signin',
  profile: '/profile',
  wishlist: '/profile/wishlist',
  orders: '/profile/orders',
  cart: '/cart',
});

export default routes;
